var config = {};

config.socket = {};
config.team = {};

config.socket.host = "hostname";
config.socket.url = "http://localhost";
config.socket.port = 3000;

config.team.name = "Team test";

module.exports = config;